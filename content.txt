1.class and obj(examples) 
2.class has data/atttribute and func/methods
   >code wakthrough
   >attributes get called itself, methods are calld manually
3.why python is called oop?--everything i python is an object(data structure,data type etc)
4.advantages of oops:
   >you can create your own datatype
   >code reusability
   >debugging
   >easy to collab
  5.end to end project
  .function vs methods
  >magic method and dunder method
   >self
   >we can create attribute outside class as well
    >encapsulation
    >getter and setter
    >static method 